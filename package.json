{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "slack chat",
  "main": "./chat/src/index.js",
  "scripts": {
    "build": "npm run build --prefix chat",
    "start": "start-server -s ./chat/build",
    "dev": "start-server -s ./chat/start",
    "postinstall": "npm ci --prefix chat",
    "test": "echo \"Error: no test specified\" && exit 1"
    
    
  },
  "author": "Tarasov Dmitrii",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.6"
  }
}
